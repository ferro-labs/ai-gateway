Keeps internal/webui/dist/ present in a fresh clone so //go:embed all:dist
resolves before anyone has built the frontend. Everything else in this
directory is generated by the build and ignored; nothing here is a build
artifact, so no build can dirty a tracked file. When index.html is absent
the handler serves the placeholder compiled into webui.go instead.
